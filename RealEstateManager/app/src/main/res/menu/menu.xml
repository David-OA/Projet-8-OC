<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/adding"
        android:icon="@drawable/add_icon"
        app:showAsAction="always"
        android:title="Add a property or agent">

        <menu>
            <item
                android:id="@+id/add_property"
                android:icon="@drawable/home_icon"
                android:title="Add a property"
                app:showAsAction="withText"/>

            <item
                android:id="@+id/add_agent"
                android:icon="@drawable/add_agent"
                android:title="Add a agent"
                app:showAsAction="withText"/>

        </menu>

    </item>

    <item
        android:id="@+id/edit"
        android:icon="@drawable/edit_icon"
        app:showAsAction="always"
        android:title="Edit"/>

    <item
        android:id="@+id/search"
        android:icon="@drawable/search_icon"
        app:showAsAction="always"
        android:title="Search"/>

</menu>